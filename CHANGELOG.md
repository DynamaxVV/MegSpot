# Changelog

## [2.2.12-vv1.0.11] - 2026-09-19

### Added
- 首页“配对预览”新增“按顺序匹配”功能：支持按每组文件夹独立开启或关闭。开启后，对应组别的文件夹图片不再按文件名匹配，而是直接按文件名升序排序后一对一依次配对（例如 `01.jpg` 与 `999.jpg` 依次配对）。
- 交互与样式统一升级：无论添加 1 组还是多组文件夹，均统一使用紧凑的气泡弹窗（Popover）按钮（`按顺序匹配 (已开启/未开启)` 或 `按顺序匹配 (X/Y)`）；点击唤起浮层独立配置每组规则，多组场景支持“全开/全关”一键批量操作。

### Fixed
- 修复多文件夹场景下配置项挤占垂直空间导致下方配对预览表格溢出被挤出屏幕的问题；优化首页弹性容器与标题栏排版，确保标题栏严格保持单行，下方配对预览表格自适应高度并在内部平滑滚动。
- 修复成对对比工作区切页时（例如从 9/10 切到 10/10）页码指示器位宽变化导致右侧按钮横向抖动的问题；通过等宽字符占位（tabular numbers 与 ch 定宽）锁定切页控件宽度，彻底消除切页布局跳动。

## [2.2.12-vv1.0.10] - 2026-09-12

### Added
- 支持 PSD 文件参与图片扫描、配对、缩略图、并排对比、单页对比和分割对比；当前读取 PSD 合成图，不提供图层浏览。
- 新增独立 PSD 解码 Worker 与共享加载器，按缩略图、显示尺寸、原图/像素检查分级解码。

### Changed
- 修复并排成对对比连续切页时重复销毁和创建左右 `ImageCanvas` 导致的明显等待；现在复用画布实例，仅在图片路径变化时重新加载。
- PSD 缓存以路径、修改时间和文件大小识别文件变化；只预加载当前对比组前后各一组 PSD，不进入普通图片的 24 张预加载池。
- 复用 ImageCanvas、分割模式和缩略图的 PSD 解码结果，并及时释放 PSD 原始缓冲区、临时 ImageData、Canvas 和 ImageBitmap。

## [2.2.12-vv1.0.9] - 2026-08-30

### Fixed
- 主页添加文件夹时，连续嵌套且每层只有一个子文件夹的结构会直接定位到最里面一层，并忽略 `.DS_Store` 等隐藏文件；同时保留含图片等其他可见内容的文件夹原有行为。
- 修复 `06-07`、`06+07`、`6-7`、`6+7` 等纯数字组合文件名无法匹配对应单页图片的问题；组合文件未匹配时也会按首个逻辑编号保留在正确位置，不再追加到对比组末尾。

### Changed
- 偏好设置页新增“显示缩放比例”选项，默认关闭；开启后可在并排对比图片左下角显示缩放比例。
- 移除成对对比连续切页成功路径的高频诊断日志，保留图片加载、解码、任务刷新及运行时错误日志，减少本地同步日志写入。
- 优化 LP 标注页序排序：预建立精确/宽松文件名和对比行索引，避免逐条扫描及多源场景的重复排序。

## [2.2.12-vv1.0.8] - 2026-08-22

### Added
- 色彩与滤镜参数跨对比组及首页导航持久化，并支持一键恢复默认设置。
- 同一功能最多支持配置两组快捷键，并增加快捷键冲突校验。

### Changed
- 左右两侧各有一张图片时支持按位置直接配对；生产环境改用本地文件加载并补充失败诊断。
- 应用采用单实例模式，重复启动时聚焦已有窗口。

### Fixed
- 修复滤镜重置时旧的延迟更新覆盖默认值，以及刷新后任务状态残留的问题。

## [2.2.12-vv1.0.7] - 2026-08-17

### Added
- LP 标注文档支持按文档页序排列对比组，保留无标注页，并支持跨侧匹配、冗余文件名匹配和同组文件夹连续显示。
- 审校模式区分“LP 中包含但无标注”和“未被 LP 收录”的图片，并将 `999` 发布页置于两类图片之间。
- 设置页支持选择左侧或右侧为基准图；单页模式支持仅有一侧图片时显示现有图片并给出提示。

### Changed
- 图片内容更新时自动刷新当前对比位置；LP 标注文档改用内容 MD5 判断变化，避免仅修改时间变化触发刷新提示。
- 审校抽屉开关、复位和模式切换时重新同步对比区域尺寸，保持图片居中；文件列表显示类型切换后立即刷新。
- 发布流程改为在 macOS runner 上交叉构建 Windows x64，并增加打包结果中的嵌套 Electron 运行时检查。
- 应用生产环境使用明确的本地 HTML 文件加载路径，并记录窗口加载失败的错误码和实际 URL。
- 首页左右栏对应位置均为单张图片时，直接按位置配成对比组，不再要求文件名一致。

### Fixed
- 修复未匹配图片按剩余顺序强行配对、同组图片混排及 LP 空页被跳过的问题。
- 修复仅一侧存在 LP 标注文档时无法利用对侧图片匹配的问题。
- 修复右侧作为基准图时首页、对比表格和审校模式仍按左侧基准处理的问题。
- 修复单页模式缺图提示位置过高及文件列表默认显示类型不生效的问题。
- 修复应用重复启动时创建多个共享状态的实例；第二次启动现在聚焦已存在的窗口并退出新进程。

## [2.2.12-vv1.0.6] - 2026-08-05

### Added
- 新增审校模式：从图源文件夹读取 TXT 标注，在成对图片工作区展示标注和评审文本。
- 新增本地诊断日志页面，可查看最近日志并打开日志文件夹。

### Changed
- 关闭运行时自动更新、在线埋点和外部网络访问入口，应用以本地运行和本地文件处理为边界。

## [2.2.12-vv1.0.5] - 2026-08-04

### Added
- 增补成对图片对比、审校模式和标注序号显示的可配置快捷键。

### Changed
- 移除原项目中当前流程不再使用的快捷键。
- 设置页将原项目信息折叠展示，减少二次开发功能说明与上游信息的混淆。

## [2.2.12-vv1.0.4] - 2026-07-27

### Added
- 首页支持同侧文件夹拖拽排序。
- 新增组合文件名和“拷贝”后缀的保守匹配规则。

### Fixed
- 成对对比组缺图时支持临时显示前一组同侧图片。

## [2.2.12-vv1.0.3] - 2026-07-25

### Added
- 成对对比进度下拉栏打开时自动滚动到当前选中组，并居中显示

### Fixed
- 修复两边文件夹图片数量不一致时，进度下拉栏无法定位到当前对比组的 bug
- 修复成对对比页面点击图片画布等区域时下拉栏不自动收起的问题

## [2.2.12-vv1.0.2] - 2026-07-24

### Added
- 新增对比组预加载策略，前后各 1 组自动后台预热

### Fixed
- 修复图片缩略图全分辨率加载导致的内存泄漏问题，新增有界 LRU 缓存（30 张）和预加载池
- 修复对比页面快速翻页时 ImageBitmap 未关闭、cv.Mat 未删除、blob URL 未释放等内存泄漏
- 修复 GPU 进程占用数 GB 问题：Canvas 销毁时归零释放后备存储，Worker 孤儿 Bitmap 关闭，预加载窗口缩窄

## [2.2.12-vv1.0.1] - 2026-07

### Added
- 图片对比页面新增空格回正功能及双击调整单图片缩放功能
- 新增高清模式

### Fixed
- 修复首页多文件夹排序问题
- 修复分割模式界面的显示问题

## [2.2.12-vv1.0.0] - 2026-07

### Added
- 新增成对图片对比工作流（PairCompareWorkspace）：支持并排、单页、分割三种对比模式
- 首页支持左右两侧分别选择或拖入图片/文件夹，自动配对预览并启动对比任务
- 配对算法：按文件名忽略扩展名和大小写精确匹配，未匹配项按排序顺序配对
- 文件夹来源按绝对路径去重，仅读取直属图片文件
- 内容变更实时检测（chokidar），文件增删重命名时标记任务到期，支持显式刷新
